<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TD6 - Kubernetes Microservices</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 20px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .container {
      background: white;
      border-radius: 10px;
      padding: 40px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.3);
      max-width: 600px;
    }
    h1 {
      color: #333;
      text-align: center;
      margin-top: 0;
    }
    .info-box {
      background: #f5f5f5;
      border-left: 4px solid #667eea;
      padding: 15px;
      margin: 20px 0;
      border-radius: 5px;
    }
    .label {
      font-weight: bold;
      color: #667eea;
    }
    .value {
      color: #333;
      margin-top: 5px;
    }
    .status {
      text-align: center;
      padding: 20px;
      background: #f0f9ff;
      border-radius: 5px;
      margin-top: 20px;
    }
    .status.success {
      color: #27ae60;
      border: 2px solid #27ae60;
    }
    .status.error {
      color: #e74c3c;
      border: 2px solid #e74c3c;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🎉 TD6: Kubernetes Microservices</h1>
    
    <div class="info-box">
      <div class="label">Frontend Service</div>
      <div class="value"><%= service %></div>
    </div>

    <div class="info-box">
      <div class="label">Timestamp</div>
      <div class="value"><%= timestamp %></div>
    </div>

    <div class="info-box">
      <div class="label">Backend Response</div>
      <div class="value"><%= backendResponse %></div>
    </div>

    <% if (error) { %>
      <div class="status error">
        ⚠️ Error: <%= error %>
      </div>
    <% } else { %>
      <div class="status success">
        ✅ Successfully connected to backend service!
      </div>
    <% } %>
  </div>
</body>
</html>
